#!/usr/bin/env node

const { generateChangelog, extractOrigin } = require('../index');
const origin = extractOrigin();

try {
  generateChangelog(origin);
} catch(e) {
  console.error(e);
  process.exit(1);
}
