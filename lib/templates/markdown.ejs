<% if (merges.length > 0 || regularCommits.length > 0) { -%>
### <%-tag%><%-date && ` (${date})`%>

<% if (merges.length === 0 || regularCommits.length === 0) { -%>
<%- include('pullRequests'); -%>
<%- include('commits'); -%>
<% } else { -%>
#### Pull requests

<%- include('pullRequests'); -%>

#### Commits to master

<%- include('commits'); -%>
<% } -%>
<% } -%>
